<Window x:Class="Govorun.Dialogs.BookInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Govorun.Dialogs"
        mc:Ignorable="d"
        Title="О книге"
        Width="500" Height="500" ResizeMode="CanResizeWithGrip"
        MinWidth="500" MinHeight="500"
        d:Width="500" d:Height="500"
        SourceInitialized="Window_SourceInitialized"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="90" />
                <Setter Property="Height" Value="25" />
            </Style>

            <Style x:Key="HyperlinkStyle" TargetType="Hyperlink">
                <Setter Property="TextDecorations" Value="None" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="TextDecorations" Value="Underline" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="TextBoxStyle" TargetType="TextBox">
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="Margin" Value="0,4,0,0" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Авторы -->
        <TextBlock Grid.Row="0" x:Name="AuthorsTextBlock"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="8,0,8,0" />

        <!-- Название -->
        <TextBlock Grid.Row="1" x:Name="TitleTextBlock"
                   TextWrapping="Wrap"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="8,10,8,0" />

        <!-- Чтец и продолжительность -->
        <Grid Grid.Row="2" Margin="8,10,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Читает:" Margin="0,0,5,0" />
                <TextBlock Grid.Column="1" x:Name="LectorTextBlock" TextWrapping="Wrap" />
            </Grid>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Время:" Margin="25,0,5,0" />
                <TextBlock x:Name="DurationTextBlock" />
            </StackPanel>
        </Grid>

        <!-- Серия и номер -->
        <Grid Grid.Row="3" x:Name="CycleGrid" Margin="8,8,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Серия:" Margin="0,0,5,0" />
                <TextBlock Grid.Column="1" x:Name="CycleTitleTextBlock" TextWrapping="Wrap" />
            </Grid>
            <StackPanel Grid.Row="1" x:Name="CycleNumbersStackPanel" Orientation="Horizontal">
                <TextBlock Text="Номера в серии:" Margin="0,0,5,0" />
                <TextBlock x:Name="CycleNumbersTextBlock" />
            </StackPanel>
        </Grid>

        <!-- Перевод -->
        <StackPanel Grid.Row="4" x:Name="TranslatorStackPanel" Orientation="Horizontal" Margin="8,4,8,0">
            <TextBlock Text="Перевод:" Margin="0,0,5,0" />
            <TextBlock x:Name="TranslatorTextBlock" />
        </StackPanel>

        <!-- Жанры -->
        <Grid Grid.Row="5" x:Name="GenressGrid" Margin="8,4,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Жанры:" Margin="0,0,5,0" />
            <TextBlock Grid.Column="1" x:Name="GenresTextBlock" TextWrapping="Wrap" />
        </Grid>

        <!-- Аннотация -->
        <GroupBox Grid.Row="6" Header="Аннотация" Margin="0,10,0,0">
            <TextBox x:Name="AnnotationTextBox"
                     BorderThickness="0"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Style="{StaticResource TextBoxStyle}" />
        </GroupBox>

        <!-- Файл -->
        <GroupBox Grid.Row="7" Header="Файл" Margin="0,10,0,0">
            <TextBox x:Name="FileTextBox"
                     BorderThickness="0"
                     TextWrapping="Wrap"
                     Height="Auto"
                     Style="{StaticResource TextBoxStyle}" />
        </GroupBox>

        <!-- Кнопки -->
        <Grid Grid.Row="8" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="PlayButton" Content="Слушать"
                    HorizontalAlignment="Left"
                    Style="{StaticResource ButtonStyle}"
                    Click="PlayButton_Click" />
                <Button x:Name="ChaptersButton" Content="Содержание"
                    HorizontalAlignment="Left"
                    Style="{StaticResource ButtonStyle}"
                    Margin="10,0,0,0"
                    Click="ChaptersButton_Click" />
                <Button x:Name="BookmarksButton" Content="Закладки"
                    HorizontalAlignment="Left"
                    Style="{StaticResource ButtonStyle}"
                    Margin="10,0,0,0"
                    Click="BookmarksButton_Click" />
            </StackPanel>
            <Button Grid.Column="1" x:Name="CloseButton" Content="Закрыть"
                    HorizontalAlignment="Right"
                    Style="{StaticResource ButtonStyle}"
                    Click="CloseButton_Click" />
        </Grid>
    </Grid>
</Window>
